let jsx = (a, b) => {
  a(b)
}

let jsxs = (a, b) => {
  a(b)
}

let array = a => a

// module Elements = {
//   type m = | @as("mesh") Mash

//   @inline
//   let jsx = (tag: string, b: pro) => {name: b.name}
// }
